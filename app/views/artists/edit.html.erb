<%= form_for @artist do |f| %>
	<fieldset>
		<ol>
			<li>
				<%= f.label :name %>
				<%= f.text_field :name %>
			</li>
			<li>
				<%= f.label :bio %>
				<%= f.text_field :bio %>
			</li>
		</ol>
	</fieldset>
	<fieldset>
		<%= submit_tag "Submit", :id => "submit-btn" %>
	</fieldset>
<% end %>